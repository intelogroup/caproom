[package]
name = "caproom-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "caproom MCP — pull-only, strictly typed enums (no push)"

[dependencies]
caproom-core = { path = "../caproom-core", version = "0.9.4" }
serde = { workspace = true }
serde_json = { workspace = true }
libc = { workspace = true }
