[package]
name = "caproom-daemon"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "caproomd — single collector + session graph + pressure source + risk arbiter (v1.0 skeleton)"

[dependencies]
caproom-core = { path = "../caproom-core" }
serde = { workspace = true }
serde_json = { workspace = true }
libc = { workspace = true }
