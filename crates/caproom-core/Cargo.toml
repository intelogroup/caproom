[package]
name = "caproom-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core policy, collector, and process-tree logic for caproom — memory-cap enforcement for any command"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
libc = { workspace = true }

[target.'cfg(target_os = "macos")'.dependencies]
# no extra deps — uses libc FFI directly
